let objeto = {
    aluno:'caio',
    idade:19,
    gosto:'rock',
}

for(let objeto.idade; objeto.idade<=20; objeto.idade++){
function aaa(objeto){
   let a =`olha so to PUTO não queria estudar mas ne to aqui olhe o resultado disso é ${objeto.idade + 1}`
    return a
}
}

let cu = aaa(objeto);

console.log(`Isso aqui é a variael ->${cu}`);
